name: My_first_workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4

            - name: hello messge
              run: echo "Hello world"

            - name: list files
              run: ls

            - name: cat file
              run: cat README.md